def get_median():
    number_array = list()
    number_index = input("Inserisci il numero dei numeri: ") 
    if number_index % 2 == 1:
        return number_array[numeber_index/2]
    elif number_index % 2 == 0:
        